<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.InterConnect" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:fontFamily">@font/kanit_regular</item>
        <item name="android:statusBarColor">@color/light_blue</item>
        <item name="android:navigationBarColor">@color/main_bg</item>

    </style>

    <style name="Theme.InterConnect" parent="Base.Theme.InterConnect" />

    <style name="myBottomNavigation">
        <item name="backgroundTint">@color/main_bg</item>
        <item name="android:textColor">@color/gray</item>
        <item name="android:iconTint">@color/light_blue</item>
        <item name="android:tint">@color/light_blue</item>
    </style>
</resources>